
// this doesnot check the datatype and will get executed
if(2=='2'){
    console.log(`execute`);
}

// when the checking of datatype is also important than we use ===
if(2==='2'){
    console.log(`exexuted`);
}